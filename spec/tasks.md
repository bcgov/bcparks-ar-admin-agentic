# Tasks — AUTH-003 (active)

- [x] Add `KeycloakService.logout()` calling `keycloakAuth.logout` with a redirect URI; unit-test it
- [x] Add authenticated header “Log out” control that invokes `KeycloakService.logout`; test as practical
- [x] Update AuthGuard comment(s) that claim there is no logout
- [x] Append `docs/pr-evidence.md` for AUTH-003
- [ ] Checkpoint 3 + merge (must change `src/`; refuse evidence-only)
