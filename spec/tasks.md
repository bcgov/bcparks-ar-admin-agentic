# Tasks — CONFIG-005 (active)

- [x] In `.github/workflows/analysis.yaml`, enable push to main, pull_request, and weekly schedule triggers (keep workflow_dispatch)
- [x] Append `docs/pr-evidence.md` for CONFIG-005
- [ ] Checkpoint 3 + merge (must change `.github/workflows/`; refuse evidence-only)
